server:
  port: 8761

eureka:
  instance:
    hostname: eureka-server # Este nombre coincide con el servicio en Docker
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    waitTimeInMsWhenSyncEmpty: 0 # Opcional para evitar problemas de inicialización

